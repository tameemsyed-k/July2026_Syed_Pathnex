
# WHAT I DID TODAY
1hr -   Understood the roadmap to become a Devops Engineer.

The DevOps Lifecycle: From Code to Cloud Deployment - High Level Overview.
Here is the exact path our code takes from laptop to the real world, 
step-by-step:
Git: Saves and tracks the history of changes made to the source code.
GitLab / Jenkins: Automatically catches your pushed code to build, test, and run the deployment pipeline.
SonarQube: Scans that source code for bugs and security vulnerabilities.
Docker: Packages your application into a standardized, portable container.
Trivy: Inspects your newly built Docker container for known security flaws.
Terraform: Automatically builds the cloud infrastructure (like AWS servers) needed to host your app.
Ansible: Configures those newly built servers by installing software and adjusting settings.
Helm: Bundles your complex Kubernetes deployment configurations into a reusable template.
Argo CD: Automatically deploys your application by syncing the live cluster with the configurations stored in Git.
Kubernetes: Runs, manages, and scales your containers to keep the app highly available.
DataDog & ELK Stack: Monitors the live application's health and logs to catch errors in real-time.


2.5 hrs -   Attended today's Class by Neeraj at 6 AM / 2nd July 2026
 summary of Class
 Neeraj explained how to create a ec2 vm, git and its configuration, and the daily activities to be done.


Practiced few git commands

`git config --global`: Configures user identity (name and email) for all local repositories.
 
`git clone`: Copies a remote repository and downloads it to the local machine.

`git status`: Displays the exact state of the working directory and shows what is currently being tracked.

`git add`: Stages specific files, telling Git to get them ready to be saved in the next snapshot.
 
`git commit -m`: Takes a snapshot of staged changes and securely attaches a descriptive message.
 
`git push`: Sends locally saved commits up to the remote repository (like GitHub).
 
`git checkout -b`: Creates a brand new isolated feature branch and switches your workspace to it immediately.

`git checkout`: Switches active workspace back to a different, already existing branch (like `main`).

`git pull`: Downloads the latest changes from the remote repository and seamlessly merges them into the current local branch.

`git merge`: Combines two branches by bringing their changes together and creating a new merge commit to tie their histories.


